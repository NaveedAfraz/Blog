{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" } // Replace with your output dir
    }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}