{
  "build": {
    "use": "@vercel/static-build",
    "src": "package.json",
    "config": {
      "distDir": "build"
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
